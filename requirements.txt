pandas>=2.0
openpyxl>=3.1
pyyaml>=6.0

# build-only — used by scripts/09_fetch_ward_boundaries.py to fetch + project +
# simplify the ONS WD24 ward boundaries into data/gm_ward_geoms.json. Not
# required at runtime; the deployed artifact (docs/map.html + the JSON) needs
# nothing more than what GitHub Pages serves statically.
geopandas>=0.14
shapely>=2.0
pyproj>=3.6

# build-only — used by scripts/_official_parsers/_cloudflare.py to impersonate a
# real Chrome TLS fingerprint so Cloudflare-gated council sites (hants.gov.uk)
# return 200 instead of 403. Not required at runtime; the deployed docs/
# artifacts never re-fetch. Pinned to the 0.7 line — if a PyPI wheel isn't
# available for the contributor's Python+arch combo, install needs a C
# toolchain (libcurl-impersonate is built from source).
curl_cffi>=0.7,<0.8
